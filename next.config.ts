import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  images: {
    domains: [
      'moccasin-large-parrot-711.mypinata.cloud',
      'res.cloudinary.com',
      'www.google.com',
      'th.bing.com',
    ],
  },
};

export default nextConfig;
